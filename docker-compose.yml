
#firefox - cfmmo
version: "3"

services:
    haproxy:
        build:
            context: .
            dockerfile: HaproxyDockerfile
        image: haproxyimg
        container_name: haproxy
        ports:
            - 2086:10000
            - 56789:9999
        volumes:
           - ./ovpn:/ovpn
        links:
            - warp:warp
            - proxy001:proxy001
            - psiphon001:psiphon001
            - riseup001:riseup001
            - freeconnect001:freeconnect001
            - ovpn_proxy_00:vpn00
            - ovpn_proxy_01:vpn01
            - ovpn_proxy_02:vpn02
            - ovpn_proxy_03:vpn03
            - ovpn_proxy_04:vpn04
            - ovpn_proxy_05:vpn05
            - ovpn_proxy_06:vpn06
            - ovpn_proxy_07:vpn07
            - ovpn_proxy_08:vpn08
            - ovpn_proxy_09:vpn09
            - ovpn_proxy_10:vpn10
            - ovpn_proxy_11:vpn11
            - ovpn_proxy_12:vpn12
            - ovpn_proxy_13:vpn13
            - ovpn_proxy_14:vpn14
            - ovpn_proxy_15:vpn15
            - ovpn_proxy_16:vpn16
            - ovpn_proxy_17:vpn17
            - ovpn_proxy_18:vpn18
            - ovpn_proxy_19:vpn19

        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.100
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"
    warp:
        image: caomingjun/warp
        restart: always
        environment:
            - WARP_SLEEP=2
            # - WARP_LICENSE_KEY= # optional
        cap_add:
            - NET_ADMIN
        sysctls:
            - net.ipv6.conf.all.disable_ipv6=0
            - net.ipv4.conf.all.src_valid_mark=1
        volumes:
            - ./data:/var/lib/cloudflare-warp

        networks:
            custom_network:
                ipv4_address: 172.21.0.101
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"
  
    psiphon001:
        build:
            context: .
            dockerfile: PsiphonDockerfile
        image: psiphonimg
        container_name: psiphon001
        hostname: psiphon001
        restart: always
        environment:
            # Leave empty for fastest/any region. Set e.g. SG, JP, US to pin egress geo.
            - EGRESS_REGION=
            # Optional: auto-fetch a fresh psiphon config from this URL on start
            # and on each refresh. Empty = always use the bundled standard config.
            # Invalid/unreachable downloads automatically fall back to standard.
            - CONFIG_URL=https://raw.githubusercontent.com/swarupsengupta2007/psiphon-docker/master/assets/psiphon.config
            # Seconds between config re-checks when CONFIG_URL is set (0 = off).
            # On change, Psiphon restarts (restart: always) to apply it.
            - CONFIG_REFRESH_INTERVAL=21600
            # URL the healthcheck fetches THROUGH the Psiphon proxy to prove egress.
            - HEALTHCHECK_URL=https://ifconfig.io/ip
        volumes:
            - ./psiphon_data:/psiphon/data
        networks:
            custom_network:
                ipv4_address: 172.21.0.132
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    riseup001:
        build:
            context: .
            dockerfile: RiseupDockerfile
        image: riseupimg
        container_name: riseup001
        hostname: riseup001
        restart: always
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        environment:
            # Seconds between gateway rotations (re-picks a random Riseup server).
            - ROTATING_DELAY=360
            # Optional: pin egress location (e.g. Paris, Amsterdam, Seattle).
            # Empty = random across all advertised Riseup gateways.
            - RISEUP_LOCATION=
            # OpenVPN transport protocol to prefer: udp (default) or tcp.
            - RISEUP_PROTO=udp
            # URL the healthcheck fetches THROUGH the proxy to prove egress.
            - HEALTHCHECK_URL=https://www.google.com/generate_204
        volumes:
            - ./riseup_data:/riseup/data
        networks:
            custom_network:
                ipv4_address: 172.21.0.133
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    freeconnect001:
        build:
            context: .
            dockerfile: FreeconnectDockerfile
        image: freeconnectimg
        container_name: freeconnect001
        hostname: freeconnect001
        restart: always
        environment:
            # Seconds between upstream rotations (re-picks a random FreeConnect proxy).
            - ROTATING_DELAY=360
            # Upstream proxy port. The CSV only lists domain/user/pass; the
            # documented example uses 9251. Override here if it differs.
            - FREECONNECT_PORT=9251
            # Credentials are identical for every server, so they can be set once
            # here. Leave EMPTY to use the per-row user/pass from the CSV instead.
            - FREECONNECT_USER=freeuser1
            - FREECONNECT_PASS=freeuser1
            # Optional: keep only hosts whose domain matches this substring
            # (e.g. us0, nl, de). Empty = random across all listed proxies.
            - FREECONNECT_FILTER=
            # URL the healthcheck fetches THROUGH the proxy to prove egress.
            - HEALTHCHECK_URL=https://ifconfig.io/ip
        volumes:
            # Mount the proxy list read-only so it can be updated without rebuild.
            - ./data:/data:ro
        networks:
            custom_network:
                ipv4_address: 172.21.0.134
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    proxy001:
        image: bluet/proxybroker2:v2.0.0-alpha6
        container_name: proxy001
        hostname: proxy001
        restart: always
        cap_add:
            - NET_ADMIN
        sysctls:
            - net.ipv6.conf.all.disable_ipv6=0
            - net.ipv4.conf.all.src_valid_mark=1
        command: serve --host 0.0.0.0 --port 8888 --types HTTP HTTPS --lvl High --strict -l 10 --min-queue 100
        stdin_open: true
        tty: true
        networks:
            custom_network:
                ipv4_address: 172.21.0.131
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"
    restarter:
        image: docker:cli
        volumes: ["/var/run/docker.sock:/var/run/docker.sock"]
        command: ["/bin/sh", "-c", "while true; do sleep 180; docker restart proxy001; done"]
        restart: always
    ovpn_proxy_00:
        build:
            context: .
            dockerfile: Dockerfile
        image: testimg
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        volumes:
            - ./ovpn:/ovpn
        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.102
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    ovpn_proxy_01:
        build:
            context: .
            dockerfile: Dockerfile
        image: testimg
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        volumes:
            - ./ovpn:/ovpn
        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.103
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    ovpn_proxy_02:
        build:
            context: .
            dockerfile: Dockerfile
        image: testimg
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        volumes:
            - ./ovpn:/ovpn
        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.104
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    ovpn_proxy_03:
        build:
            context: .
            dockerfile: Dockerfile
        image: testimg
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        volumes:
            - ./ovpn:/ovpn
        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.105
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    ovpn_proxy_04:
        build:
            context: .
            dockerfile: Dockerfile
        image: testimg
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        volumes:
            - ./ovpn:/ovpn
        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.106
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    ovpn_proxy_05:
        build:
            context: .
            dockerfile: Dockerfile
        image: testimg
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        volumes:
            - ./ovpn:/ovpn
        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.107
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    ovpn_proxy_06:
        build:
            context: .
            dockerfile: Dockerfile
        image: testimg
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        volumes:
            - ./ovpn:/ovpn
        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.108
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    ovpn_proxy_07:
        build:
            context: .
            dockerfile: Dockerfile
        image: testimg
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        volumes:
            - ./ovpn:/ovpn
        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.109
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    ovpn_proxy_08:
        build:
            context: .
            dockerfile: Dockerfile
        image: testimg
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        volumes:
            - ./ovpn:/ovpn
        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.110
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    ovpn_proxy_09:
        build:
            context: .
            dockerfile: Dockerfile
        image: testimg
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        volumes:
            - ./ovpn:/ovpn
        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.111
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"


    ovpn_proxy_10:
        build:
            context: .
            dockerfile: Dockerfile
        image: testimg
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        volumes:
            - ./ovpn:/ovpn
        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.112
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    ovpn_proxy_11:
        build:
            context: .
            dockerfile: Dockerfile
        image: testimg
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        volumes:
            - ./ovpn:/ovpn
        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.113
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    ovpn_proxy_12:
        build:
            context: .
            dockerfile: Dockerfile
        image: testimg
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        volumes:
            - ./ovpn:/ovpn
        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.114
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    ovpn_proxy_13:
        build:
            context: .
            dockerfile: Dockerfile
        image: testimg
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        volumes:
            - ./ovpn:/ovpn
        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.115
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    ovpn_proxy_14:
        build:
            context: .
            dockerfile: Dockerfile
        image: testimg
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        volumes:
            - ./ovpn:/ovpn
        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.116
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    ovpn_proxy_15:
        build:
            context: .
            dockerfile: Dockerfile
        image: testimg
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        volumes:
            - ./ovpn:/ovpn
        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.117
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    ovpn_proxy_16:
        build:
            context: .
            dockerfile: Dockerfile
        image: testimg
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        volumes:
            - ./ovpn:/ovpn
        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.118
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    ovpn_proxy_17:
        build:
            context: .
            dockerfile: Dockerfile
        image: testimg
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        volumes:
            - ./ovpn:/ovpn
        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.119
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    ovpn_proxy_18:
        build:
            context: .
            dockerfile: Dockerfile
        image: testimg
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        volumes:
            - ./ovpn:/ovpn
        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.120
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"

    ovpn_proxy_19:
        build:
            context: .
            dockerfile: Dockerfile
        image: testimg
        cap_add:
            - NET_ADMIN
        devices:
            - /dev/net/tun
        dns:
            - 1.1.1.1
        volumes:
            - ./ovpn:/ovpn
        restart: always
        networks:
            custom_network:
                ipv4_address: 172.21.0.121
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "1"
networks:
    custom_network:
        ipam:
            driver: default
            config:
                - subnet: 172.21.0.0/24
                  gateway: 172.21.0.1
